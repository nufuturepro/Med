name: E2

on:
  push:
    branches: [main]

jobs:
  e2:
    runs-on: ubuntu-latest
    steps:
      - name: JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Echo
        run: echo "e2 ok"
