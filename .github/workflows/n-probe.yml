name: ReleaseN

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Env with only the keystore blob
        env:
          KEY_STORE_FILE: ${{ secrets.KEY_STORE_FILE }}
        run: echo "probe N"
